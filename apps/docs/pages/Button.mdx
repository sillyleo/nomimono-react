# Button

import { Button, LucideIcon, Text } from "@sayaww/nomi-mono";

<Button leftIcon={<LucideIcon name="plus" />}>Hello</Button>
<Button intent={"secondary"} leftIcon={<LucideIcon name="plus" />}>Hello</Button>

<Text size={"heading-l"}>Hello</Text>

```jsx
import { Button, LucideIcon, Text } from "@sayaww/nomi-mono";
import "@sayaww/nomi-mono/style";

<Button leftIcon={<LucideIcon name="plus" />}>Hello</Button>
<Text size={"heading-l"}>Hello</Text>
```